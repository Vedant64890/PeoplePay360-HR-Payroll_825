{
  "name": "odoo-final-hackathon-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
  "dev": "concurrently -n frontend,backend \"npm --prefix frontend run dev\" \"npm --prefix backend run dev\"",

  "db:sync": "npm --prefix backend run prisma:sync"
},
  "devDependencies": {
    "concurrently": "^9.2.4"
  }
}
