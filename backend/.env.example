DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/Odoo_hackathon?schema=auth"
FRONTEND_URL="http://localhost:3000"
# Password-reset email delivery (use your provider's SMTP settings).
SMTP_HOST=""
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=""
SMTP_PASSWORD=""
MAIL_FROM="PeoplePay360 <no-reply@your-domain.com>"

# Private bank storage: generate 32 random bytes as 64 hexadecimal characters.
BANK_ENCRYPTION_KEY=""
# Optional persistent private PDF directory (defaults to backend/.data/payslips).
# PAYSLIP_STORAGE_DIR="/private/peoplepay360/payslips"
# Optional TrueType font for Unicode employee names.
# PDF_FONT_PATH="/usr/share/fonts/truetype/dejavu/DejaVuSans.ttf"
